name: Blueprint Cross Evidence Boundary Convergence

on:
  pull_request:
    paths:
      - "lib/blueprints/engine/**"
      - "app/api/blueprints/**/engine-benchmark/route.ts"
      - ".github/workflows/blueprint-cross-evidence-boundary-convergence.yml"

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Cross-evidence boundary convergence contract
        run: npx -y tsx lib/blueprints/engine/cross-evidence-boundary-convergence-diagnostic.contract.test.ts
