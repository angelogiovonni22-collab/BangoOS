export default function AppEntryLoading() {
  return (
    <main
      aria-label="Opening B.O.S."
      style={{
        position: "fixed",
        inset: 0,
        minHeight: "100dvh",
        background: "#000000",
      }}
    />
  );
}
