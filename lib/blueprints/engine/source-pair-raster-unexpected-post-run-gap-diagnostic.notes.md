# Unexpected post-run raster gap diagnostic

Read-only Phase 1 diagnostic for source faces already proven to survive the production min-run threshold and to have no retained same-band de-duplication competitor. It classifies only nearby production raster-run evidence and always fails closed; it does not authorize extraction, selection, canonical geometry, persistence, or 3D changes.
