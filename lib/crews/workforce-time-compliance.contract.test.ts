import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{resolve}from"node:path";const service=readFileSync(resolve(process.cwd(),"lib/crews/workforce-time.ts"),"utf8"),surface=readFileSync(resolve(process.cwd(),"components/crews/mobile-time-approvals.tsx"),"utf8");assert.match(service,/Math\.max\(0,entry\.hours-8\)/);assert.match(service,/entry\.hours>=6&&entry\.breakMinutes===0/);assert.match(surface,/Missing break record/);assert.match(surface,/overtime hours require review/);console.log("Workforce time compliance contract checks passed.");
