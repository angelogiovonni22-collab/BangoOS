import assert from "node:assert/strict";import{readFileSync}from"node:fs";import{resolve}from"node:path";
const root=process.cwd(),types=readFileSync(resolve(root,"lib/crews/mobile-field-operations-types.ts"),"utf8"),service=readFileSync(resolve(root,"lib/crews/mobile-field-operations-service.ts"),"utf8"),workspace=readFileSync(resolve(root,"components/crews/mobile-field-operations-workspace.tsx"),"utf8");
assert.match(types,/delayDurationHours/);assert.match(service,/category: source\.draft\.delayCategory/);assert.match(service,/Math\.max\(0, Number\(source\.draft\.delayDurationHours\)/);assert.match(service,/correctiveAction: source\.draft\.delayCorrectiveAction/);assert.match(workspace,/Delay category/);assert.match(workspace,/Delay corrective action/);console.log("Structured field delay reporting contract checks passed.");
