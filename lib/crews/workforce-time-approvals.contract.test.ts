import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=process.cwd(),service=readFileSync(resolve(root,"lib/crews/workforce-time.ts"),"utf8"),surface=readFileSync(resolve(root,"components/crews/mobile-time-approvals.tsx"),"utf8"),workspace=readFileSync(resolve(root,"components/crews/mobile-field-operations-workspace.tsx"),"utf8");assert.match(service,/eq\("company_id",workspace\.context\.companyId\)/);assert.match(service,/status:decision/);assert.match(service,/approved_by:workspace\.context\.userId/);assert.match(service,/breakMinutes\/60/);assert.match(surface,/Approve/);assert.match(surface,/Reject/);assert.match(workspace,/MobileTimeApprovals/);console.log("Workforce time approval contract checks passed.");
