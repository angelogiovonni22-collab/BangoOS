import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{resolve}from"node:path";const root=process.cwd(),migration=readFileSync(resolve(root,"supabase/migrations/20260814113000_workforce_time_entries.sql"),"utf8"),service=readFileSync(resolve(root,"lib/crews/workforce-time.ts"),"utf8"),workspace=readFileSync(resolve(root,"components/crews/mobile-field-operations-workspace.tsx"),"utf8");assert.match(migration,/enable row level security/);assert.match(migration,/one_open_employee/);assert.match(migration,/record_workforce_time_event/);assert.match(migration,/created_by=auth\.uid\(\)/);assert.match(service,/resolveWorkspaceContext/);assert.match(workspace,/MobileWorkforceTimeClock/);console.log("Workforce time and attendance contract checks passed.");
