import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{resolve}from"node:path";const service=readFileSync(resolve(process.cwd(),"lib/crews/mobile-field-operations-service.ts"),"utf8"),surface=readFileSync(resolve(process.cwd(),"components/crews/mobile-field-operations-workspace.tsx"),"utf8");for(const id of["check-in","field-photos","field-inspections","time-approvals","checklist","mobile-report","equipment"]) {assert.match(service,new RegExp(`id: "${id}"`));assert.match(surface,new RegExp(`id="${id}"`));}console.log("Mobile field navigation contract checks passed.");
