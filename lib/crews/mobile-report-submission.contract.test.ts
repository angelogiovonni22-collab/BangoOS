import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{resolve}from"node:path";const surface=readFileSync(resolve(process.cwd(),"components/crews/mobile-field-operations-workspace.tsx"),"utf8");assert.match(surface,/reportSubmissionRef\.current/);assert.match(surface,/if \(reportSubmissionRef\.current\) return/);assert.match(surface,/result && status === "submitted"/);assert.match(surface,/setMobileReport\(createEmptyMobileDailyReportDraft\(\)\)/);assert.match(surface,/submitReport\("draft"\)/);assert.match(surface,/submitReport\("submitted"\)/);console.log("Mobile report submission lifecycle contract checks passed.");
