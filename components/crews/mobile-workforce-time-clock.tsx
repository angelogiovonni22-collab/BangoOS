"use client";
import{useState}from"react";import{Clock}from"lucide-react";import{Button,Input,Select}from"@/components/ui";import{recordWorkforceTimeEvent}from"@/lib/crews/workforce-time";
export function MobileWorkforceTimeClock({employees,projectId,assignmentId}:{employees:Array<{id:string;name:string}>;projectId:string;assignmentId:string}){const[selected,setSelected]=useState(employees[0]?.id||"");const[notes,setNotes]=useState("");const[busy,setBusy]=useState(false);const[message,setMessage]=useState<string|null>(null);const run=async(action:"clock_in"|"clock_out")=>{if(!selected||busy)return;setBusy(true);setMessage(null);try{await recordWorkforceTimeEvent({employeeId:selected,action,projectId,assignmentId,notes});setMessage(action==="clock_in"?"Shift clock-in recorded.":"Shift submitted for approval.");setNotes("");}catch(error){setMessage(error instanceof Error?error.message:"Unable to record time.");}finally{setBusy(false);}};return <section data-orion-region="mobile-workforce-time" className="rounded-[var(--radius-card)] border border-[var(--border-subtle)] bg-[var(--surface-default)] p-3"><div className="flex items-center gap-2"><Clock className="h-4 w-4"/><p className="text-sm font-semibold text-[var(--text-primary)]">Workforce time clock</p></div><p className="mt-1 text-xs text-[var(--text-secondary)]">Project-linked attendance with supervisor approval.</p><Select className="mt-3" aria-label="Employee time clock" value={selected} onChange={event=>setSelected(event.target.value)}><option value="">Select employee</option>{employees.map(employee=><option key={employee.id} value={employee.id}>{employee.name}</option>)}</Select><Input className="mt-2" aria-label="Time entry notes" placeholder="Optional time note" value={notes} onChange={event=>setNotes(event.target.value)}/><div className="mt-2 grid grid-cols-2 gap-2"><Button disabled={!selected||busy} onClick={()=>void run("clock_in")}>Clock in</Button><Button variant="secondary" disabled={!selected||busy} onClick={()=>void run("clock_out")}>Clock out</Button></div>{message?<p role="status" className="mt-2 text-xs text-[var(--text-secondary)]">{message}</p>:null}</section>}
