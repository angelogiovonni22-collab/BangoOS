"use client";import{useCallback,useEffect,useState}from"react";import{Button}from"@/components/ui";import{listSubmittedWorkforceTime,reviewWorkforceTime,type WorkforceTimeReview}from"@/lib/crews/workforce-time";
export function MobileTimeApprovals(){const[rows,setRows]=useState<WorkforceTimeReview[]>([]),[busy,setBusy]=useState(""),[message,setMessage]=useState<string|null>(null);const load=useCallback(async()=>{try{setRows(await listSubmittedWorkforceTime());}catch(error){setMessage(error instanceof Error?error.message:"Unable to load time approvals.");}},[]);useEffect(()=>{const timer=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(timer);},[load]);const decide=async(id:string,decision:"approved"|"rejected")=>{setBusy(id);setMessage(null);try{await reviewWorkforceTime(id,decision);setMessage(`Time entry ${decision}.`);await load();}catch(error){setMessage(error instanceof Error?error.message:"Unable to review time entry.");}finally{setBusy("");}};return <section data-orion-region="mobile-time-approvals" className="rounded-[var(--radius-card)] border border-[var(--border-subtle)] bg-[var(--surface-default)] p-3"><p className="text-sm font-semibold text-[var(--text-primary)]">Time approvals</p><p className="mt-1 text-xs text-[var(--text-secondary)]">Review submitted project-linked shifts.</p><div className="mt-3 space-y-2">{rows.map(row=><article key={row.id} className="rounded-[var(--radius-card)] border border-[var(--border-subtle)] bg-[var(--surface-raised)] p-3"><p className="text-sm font-medium text-[var(--text-primary)]">{row.employeeName} · {row.hours.toFixed(2)} hours</p><p className="text-xs text-[var(--text-secondary)]">{new Date(row.startedAt).toLocaleString()} · Break {row.breakMinutes} min</p>{row.notes?<p className="mt-1 text-xs text-[var(--text-secondary)]">{row.notes}</p>:null}<div className="mt-2 flex gap-2"><Button size="sm" disabled={busy===row.id} onClick={()=>void decide(row.id,"approved")}>Approve</Button><Button size="sm" variant="danger" disabled={busy===row.id} onClick={()=>void decide(row.id,"rejected")}>Reject</Button></div></article>)}{!rows.length?<p className="text-xs text-[var(--text-secondary)]">No submitted time entries awaiting review.</p>:null}</div>{message?<p role="status" className="mt-2 text-xs text-[var(--text-secondary)]">{message}</p>:null}</section>}
