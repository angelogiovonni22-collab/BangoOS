"use client";import{useEffect,useMemo,useState}from"react";import{BrainCircuit,ShieldAlert}from"lucide-react";import{createClient}from"@/lib/supabase/client";import{loadBlueprintMarkups}from"@/lib/blueprints/markups";import{analyzeBlueprint,type PlanInsight}from"@/lib/blueprints/intelligence";
export function BlueprintOrionIntelligence({companyId,projectId,versionId}:{companyId:string;projectId:string;versionId:string}){const[insights,setInsights]=useState<PlanInsight[]>([]),[loading,setLoading]=useState(true),[answer,setAnswer]=useState("");useEffect(()=>{const supabase=createClient();if(!supabase)return;let active=true;void loadBlueprintMarkups(supabase,{companyId,projectId,versionId}).then(items=>{if(active)setInsights(analyzeBlueprint(items));}).finally(()=>{if(active)setLoading(false);});return()=>{active=false;};},[companyId,projectId,versionId]);const summary=useMemo(()=>insights.map(item=>`${item.severity}: ${item.title} — ${item.detail}`).join("\n"),[insights]);const ask=async()=>{setLoading(true);try{const response=await fetch("/api/orion/intelligence",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({input:`Review this Blueprint risk register and provide concise construction coordination priorities. Do not invent facts.\n${summary}`,context:{pathname:"/blueprints",projectId},tier:"deep"})});const data=await response.json() as Record<string,unknown>;setAnswer(response.ok?JSON.stringify(data.route??data.command??data):String(data.error??"Orion could not analyze this plan."));}finally{setLoading(false);}};return <aside className="rounded-lg border border-cyan-300/20 bg-slate-950 p-3 text-white" data-orion-region="blueprint-plan-intelligence"><div className="flex items-center gap-2"><BrainCircuit size={15} className="text-cyan-300"/><strong className="text-xs">Orion plan intelligence</strong><button type="button" disabled={loading} onClick={()=>void ask()} className="ml-auto rounded border border-cyan-300/30 px-2 py-1 text-[10px] font-semibold text-cyan-200 disabled:opacity-40">Deep review</button></div><div className="mt-2 grid gap-2 sm:grid-cols-2">{insights.map(item=><article key={item.id} className="rounded bg-white/5 p-2"><p className="flex items-center gap-1 text-xs font-semibold">{item.severity!=="info"?<ShieldAlert size={12} className={item.severity==="critical"?"text-red-400":"text-amber-300"}/>:null}{item.title}</p><p className="mt-1 text-[10px] text-slate-400">{item.detail}</p></article>)}</div>{answer?<p className="mt-2 whitespace-pre-wrap rounded bg-cyan-950/50 p-2 text-[10px] text-cyan-100">{answer}</p>:null}</aside>}
