"use client";
import {useEffect,useRef,useState} from"react";import{Camera,CloudOff,ScanLine}from"lucide-react";import{queueBlueprintFieldDraft,listBlueprintFieldDrafts}from"@/lib/blueprints/field-offline";
export function BlueprintFieldTools({companyId,projectId,versionId}: {companyId:string;projectId:string;versionId:string}){const camera=useRef<HTMLInputElement>(null);const[count,setCount]=useState(0),[online,setOnline]=useState(true),[message,setMessage]=useState("");const identity={companyId,projectId,versionId};useEffect(()=>{const refresh=()=>setOnline(navigator.onLine);refresh();addEventListener("online",refresh);addEventListener("offline",refresh);void listBlueprintFieldDrafts(identity).then(items=>setCount(items.length));return()=>{removeEventListener("online",refresh);removeEventListener("offline",refresh);};},[companyId,projectId,versionId]);const capture=async(file:File|null)=>{if(!file)return;await queueBlueprintFieldDraft({...identity,page:1,kind:"media",payload:{name:file.name,type:file.type,size:file.size,capturedAt:new Date().toISOString()}});setCount(value=>value+1);setMessage("Field photo queued securely on this device.");};const scan=async()=>{const xr=(navigator as Navigator&{xr?:{isSessionSupported:(mode:string)=>Promise<boolean>}}).xr;const supported=Boolean(xr&&await xr.isSessionSupported("immersive-ar"));await queueBlueprintFieldDraft({...identity,page:1,kind:"scan",payload:{mode:supported?"webxr":"photogrammetry",device:navigator.userAgent,anchors:[]}});setCount(value=>value+1);setMessage(supported?"AR/LiDAR scan session staged.":"Photo scan session staged.");};return <div className="flex flex-wrap items-center gap-2 rounded-lg border border-white/10 bg-slate-950 px-3 py-2 text-xs text-white" data-orion-region="blueprint-field-tools"><input ref={camera} className="sr-only" type="file" accept="image/*" capture="environment" onChange={event=>void capture(event.target.files?.[0]??null)}/><button type="button" onClick={()=>camera.current?.click()} className="inline-flex items-center gap-1 rounded border border-white/15 px-2 py-1"><Camera size={13}/>Field photo</button><button type="button" onClick={()=>void scan()} className="inline-flex items-center gap-1 rounded border border-white/15 px-2 py-1"><ScanLine size={13}/>Room scan</button><span className="ml-auto inline-flex items-center gap-1 text-slate-300">{!online?<CloudOff size={13}/>:null}{online?"Online":"Offline-ready"} · {count} queued</span>{message?<span role="status" className="w-full text-cyan-300">{message}</span>:null}</div>}
